# HYO Email Project Rules (.antigravity_rules.md)

이 저장소(`hyo_email`)는 이메일 사전등록 발송 HTML 템플릿의 자동 배포 및 관리를 수행합니다.

## 핵심 가이드라인 및 프로세스
- `/email-presign` 스킬 5단계 파이프라인 지침(`.agents/skills/email-presign/SKILL.md`)을 준수합니다.
- 서버 가동 시 `C:\Python37\python.exe server.py` 명령어를 사용합니다.
- Windows 콘솔 인코딩(`cp949`) 에러를 막기 위해 서버 로깅에 이모지를 배제합니다.
- 웹 픽커(`http://127.0.0.1:5000/picker`)에서 좌표 드래그 및 이동 URL(`target_url`) 설정을 가변적으로 제어하며, 배포 완료 후 브라우저 및 로컬 API에서 다운로드를 지원합니다.
